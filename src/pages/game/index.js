import React from 'react';
import * as PropTypes from "prop-types";

function Game(props) {
    return (
        <div>

        </div>
    )
}

Game.propTypes = {
    id: PropTypes.string,
}

export default Game;
